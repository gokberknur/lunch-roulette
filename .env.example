# Get a free key at https://www.maptiler.com/cloud/ (no credit card required, 100k tile loads/mo).
# Without a key, the app falls back to OSM raster tiles — fine for dev, NOT for production.
PUBLIC_MAPTILER_KEY=
